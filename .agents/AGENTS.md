# UI and Styling Guidelines
- **Strict Color Consistency**: Always ensure that newly created or modified UI elements strictly follow the system's global color variables defined in the main CSS file (e.g., `var(--primary-color)`). Never hardcode random colors or invent new HEX/RGB codes without checking the existing design system (EduMate's primary color is Rust Orange `#ce7a58`).
